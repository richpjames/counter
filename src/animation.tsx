import { keyframes } from "styled-components";

export const letterDropDown = keyframes`
0% {transform: translateY(-40%);}
100% {transform: translateY(0);}
`;

export const smileDropDown = keyframes`
0% {transform: translateY(-90%);}
100% {transform: translateY(0);}
`;
